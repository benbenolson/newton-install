#%Module1.0#####################################################################

proc ModulesHelp { } {
        global version
        puts stderr "\tThis module loads the Intel MPI Benchmarking Suite \n"
        puts stderr "\n\tVersion $version\n"
}

module-whatis   "loads IMB into the working environment."

# for Tcl script use only
set     topdir       /data/apps/imb
set     version      "3.2.4"

prepend-path  PATH              $topdir/$version/bin
