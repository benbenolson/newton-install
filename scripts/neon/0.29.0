#%Module1.0#####################################################################

proc ModulesHelp { } {
        global version
        puts stderr "\tThis module loads Neon version $version \n"
}

module-whatis   "loads Neon into the working environment."

# for Tcl script use only
set     topdir       /data/apps/neon
set     version      "0.29.0"

setenv NEON_INSTALLDIR $topdir/$version

